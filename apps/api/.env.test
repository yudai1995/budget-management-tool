# テスト専用 DB 接続設定
# docker-compose.test.yml の db_test サービスに対応
DB_HOSTNAME=127.0.0.1
DB_PORT=3307
DB_USER=Admin
DB_PASSWORD=password
DB_NAME=budgetdb_test
NODE_ENV=test
# JWT 鍵はテストコード内で generateKeyPairSync により動的生成するため不要
